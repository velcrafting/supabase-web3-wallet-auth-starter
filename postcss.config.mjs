/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},    // Make sure Tailwind CSS is enabled
    autoprefixer: {},   // Enable Autoprefixer for CSS compatibility
  },
};

export default config;
